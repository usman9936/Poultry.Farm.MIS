@model IEnumerable<Employee>

@{ 
    /* Layout = "~/Views/Shared/_Layout.cshtml"; Use Layout directive to add layout or make use _ViewsStart.cshtml to add layout*/
    ViewBag.Title = "Employee List";
}
    @*<table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Department</th>
            </tr>
        </thead>
        <tbody>

            @foreach (var employee in Model)
            {
                <tr>
                    <td>@employee.EmployeeId</td>
                    <td>@employee.Name</td>
                    <td>@employee.Department</td>
                </tr>
            }

        </tbody>
    </table>*@

    <div class="row justify-content-center m-3">
        @foreach (var employee in Model)
        { 
        <div class="card m-3" style="min-width:20rem; max-width:25%">
            <div class="card-header text-center">
                <h3>@employee.Name</h3> 
            </div>
            @*asp-append-version="true" it serves the updated content in browser if there's any update'*@ 
            <img class="card-img-top" src="~/images/No_image.jpg" asp-append-version="true"/>

            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="Details" asp-route-id="@employee.EmployeeId"
                   class="btn btn-primary">View</a>
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-primary">Delete</a>
            </div>
        </div>
        }
    </div>
